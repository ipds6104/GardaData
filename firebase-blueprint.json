{
  "entities": {
    "classification_entry": {
      "title": "Classification Entry",
      "description": "A record representing KBLI and KBJI classification mapping based on activity descriptions.",
      "type": "object",
      "properties": {
        "mjj_occtle": { "type": "string", "description": "Description of the job activity." },
        "mjj_occmtd": { "type": "string", "description": "Description of the product produced." },
        "mjj_bidang": { "type": "string", "description": "Description of the business field." },
        "mjj_kbji_label": { "type": "string", "description": "Mapping to KBJI label." },
        "mjj_kbli_label": { "type": "string", "description": "Mapping to KBLI label." },
        "createdAt": { "type": "string", "format": "date-time" },
        "createdBy": { "type": "string", "description": "Username of the person who created the entry." }
      },
      "required": ["mjj_occtle", "mjj_kbji_label", "createdAt", "createdBy"]
    },
    "village_stat": {
      "title": "Village Statistic",
      "description": "Population and general statistics for a village.",
      "type": "object",
      "properties": {
        "village": { "type": "string" },
        "male": { "type": "number" },
        "female": { "type": "number" },
        "total": { "type": "number" },
        "kk": { "type": "number" },
        "agriFamily": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["village", "total"]
    },
    "infrastructure_item": {
      "title": "Infrastructure Item",
      "description": "Detailed infrastructure items in a village.",
      "type": "object",
      "properties": {
        "category": { "type": "string" },
        "item": { "type": "string" },
        "village": { "type": "string" },
        "source": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["category", "item", "village"]
    }
  },
  "firestore": {
    "/classifications": {
      "schema": "classification_entry",
      "description": "Collection for training and reference data for KBLI and KBJI."
    },
    "/village_stats": {
      "schema": "village_stat",
      "description": "General statistics for villages in Mempawah."
    },
    "/infrastructure_items": {
      "schema": "infrastructure_item",
      "description": "List of schools, health centers, etc. per village."
    }
  }
}
